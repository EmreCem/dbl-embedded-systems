interface ILED {
  in void turnOn();
  in void turnOff();
//asdhaos
  behaviour {
    on turnOn: {}
    on turnOff: {}
  }
}
component LED {
   provides ILED iLed; // removed the component's behaviour block for now
}